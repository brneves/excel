<?php

use Illuminate\Database\Seeder;
use Illuminate\Support\Facades\DB;

class IsencaoCompleta extends Seeder
{
    public function run()
    {
        //ini_set('memory_limit', '-1');
        
        //$path = public_path('/storage/sql/isencao.sql');
        //$sql = file_get_contents($path);
        //DB::unprepared($sql);

    }
}